# Atlas Workspace Orchestrator

Extends dependency analysis and symbol reachability maps across multiple repositories and monorepo packages.


## Ecosystem Relationship
- **Module Role**: `atlas` acts as the multi-repository and monorepo workspace orchestrator, feeding reachability topology into [`nexus`](../nexus) DAG planning.
